<!-- res/drawable/ic_image_placeholder.xml -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="48dp"
    android:height="48dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="?attr/colorControlNormal">

    <!-- Основной квадрат (фон) -->
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M3,3v18h18V3H3z"
        android:strokeColor="@android:color/darker_gray"
        android:strokeWidth="1"/>

    <!-- Горизонтальная линия -->
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M3,12h18"
        android:strokeColor="@android:color/darker_gray"
        android:strokeWidth="1"/>

    <!-- Вертикальная линия -->
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M12,3v18"
        android:strokeColor="@android:color/darker_gray"
        android:strokeWidth="1"/>

    <!-- Иконка изображения (упрощенная) -->
    <path
        android:fillColor="@android:color/darker_gray"
        android:pathData="M8,5v3H5v10h14V8h-3V5H8z"/>
    <path
        android:fillColor="@android:color/darker_gray"
        android:pathData="M16,5v3h3"/>
</vector>